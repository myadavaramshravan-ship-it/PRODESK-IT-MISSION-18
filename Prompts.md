## Used prompts during project
# AURA ENGINE
1. Tell me about that inventory website categories
2. Give the information to implement a webservice for inventory
3. Aggregation implementation in MongoDB
4. Data model products in the 10000+ inventory.
5. KPI cards providing in the section format and inventory single schema products.
6. Product schema with `productName`, `sku`, `category`, `price`, `cost`, `stockQuantity`, `reorderLevel`, and `lastUpdated` in one table      format and it will be categorized from mock.
7. explain creation indexing in mongodb for objects.
8.  Validation for business rules such as:
  - `price >= cost`
  - `stockQuantity >= 0` how it is work?
9. Aggregated analytics for totals, restock priority, and category valuation using MongoDB pipelines.